package plane_data_t;

/* angles and angular_rates are based on Euler angles
 of pitch, roll, yaw. */
struct vicon_state {
    double position[3];
    double angles[3];
    double velocity[3];
    double angular_rates[3];
}

/* actuations to be converted to PWM and sent to the arduino */
struct control_commands {
    double channels[6];
}
